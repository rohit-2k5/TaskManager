# Task Manager - Frontend

Simple React + Vite frontend for the Task Manager assignment.

Run
1. cd frontend
2. npm install
3. npm run dev

The app expects backend at http://localhost:8000/tasks

Notes
- Keep backend running on port 8000 as configured in backend/.env
